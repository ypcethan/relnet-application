<% if form.errors.any? %>
  <div class="alert alert-danger form-errors">
    <h4><%= pluralize(form.errors.count, "error") %> prohibited this from being saved:</h4>
    <ul>
      <% form.errors.full_messages.each do |message|  %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
